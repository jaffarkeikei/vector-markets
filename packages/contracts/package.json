{
  "name": "@vector/contracts",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "echo 'Skipping Anchor build - run anchor build manually if Anchor CLI is installed'",
    "build:anchor": "anchor build",
    "test": "anchor test",
    "deploy:devnet": "anchor deploy --provider.cluster devnet",
    "deploy:mainnet": "anchor deploy --provider.cluster mainnet"
  },
  "devDependencies": {
    "@coral-xyz/anchor": "^0.30.1",
    "@solana/web3.js": "^1.95.0",
    "@types/chai": "^4.3.0",
    "@types/mocha": "^10.0.0",
    "chai": "^4.3.0",
    "mocha": "^10.0.0",
    "ts-mocha": "^10.0.0",
    "typescript": "^5.3.0"
  }
}
